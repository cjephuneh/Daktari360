import { Users, FileText, BarChart, Loader2, Video } from 'lucide-react'

export const Icons = {
  users: Users,
  fileText: FileText,
  barChart: BarChart,
  spinner: Loader2,
  video: Video,
}